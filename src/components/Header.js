import { Typography } from "@mui/material";

const Header = () => {
  return (
    <div>
      <Typography>[Making your Life Easier]</Typography>
      <Typography>Discovering the World</Typography>
    </div>
  );
}

export default Header;